# This is a comment

#set -g default-terminal "screen-256color"

# TEST
#set -g status-style "bg=colour0,fg=colour7"

#-------------------------------------------------------------------------------
# Key bindings
#-------------------------------------------------------------------------------

# Use 'a' as command key
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Use 'r' to reload this config file
bind r source-file ~/.tmux.conf

#---------------------------------------
# vim - by Daniel Thau
#---------------------------------------

# split windows like vim
# vim's definition of a horizontal/vertical split is reversed from tmux's
bind s split-window -v
bind v split-window -h

# move around panes with hjkl, as one would in vim after pressing ctrl-w
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# resize panes like vim
# the number stands for number of lines to be resized at each step
bind < resize-pane -L 10
bind > resize-pane -R 10
bind - resize-pane -D 10
bind + resize-pane -U 10

# bind : to command-prompt like vim
# this is the default in tmux already
#bind : command-prompt

# vi-style controls for copy mode
setw -g mode-keys vi

#-------------------------------------------------------------------------------
# Misc settings
#-------------------------------------------------------------------------------

# Start window indexing from 1 instead of 0
set -g base-index 1

#-------------------------------------------------------------------------------
# Status bar
#-------------------------------------------------------------------------------

# Display status bar
set -g status on

# Refresh rate in seconds
set -g status-interval 3

# Window list justification
set -g status-justify centre

#---------------------------------------
# Left status
#---------------------------------------

# normal
set -g status-left-style "bg=colour0,fg=colour7"

# Left status max length
set -g status-left-length 80

# Left status contents
# Disks
set -g status-left "/:#(df -h | gawk '\$NF==\"/\"{printf ""\"%%3i% \""", $5}') "
#set -ag status-left "/mnt/data:#(df -h | gawk '\$NF==\"/mnt/data\"{printf ""\"%%3i% \""", $5}')"

#---------------------------------------
# Right status
#---------------------------------------

# normal
set -g status-right-style "bg=colour0,fg=colour7"

# Right status max length
set -g status-right-length 80

# Volume
#set -g status-right "V:#(amixer get Master | gawk -F'[]%[]' '/%/{printf \"%%3i%\", $2; exit}') "
set -g status-right "V:TBD "
# Date and time
set -ag status-right "#(date '+%%d.%%m.%%y %%H:%%M')"

