#!/bin/bash

# This is a stub of a script to launch yaft with background and shell preconfigured...
# At the moment the file is added as a workaround to not being able to add empty
# bin directory to a git repository
echo "TODO Implement..."
